# ZE CenterOS v1.4.1 — Placement Layout Fix

Fixes the Placement Test action UI:
- removes floating absolute 720px popovers from Placement cards only;
- each action opens inline inside its own student card;
- open action spans full card width;
- forms use responsive 2-column layout on desktop and 1-column on mobile;
- buttons no longer float outside the card or create horizontal overflow;
- global FormDetails behavior for other modules remains unchanged.
