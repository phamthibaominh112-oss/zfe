# ZE CenterOS v1.2.3 — Teacher hourly-rate form layout fix

- Displays one teacher per full-width row so fields and the Save button are never clipped.
- Makes the Save button full and readable instead of inheriting the tiny global small-button style.
- Adds responsive layouts for tablet and mobile screens.
- Applies the same fix to Finance → Expenses and Payroll.
- No database migration is required.
