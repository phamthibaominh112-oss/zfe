import { NextResponse } from "next/server";import { requireRole } from "@/lib/auth";import { createClient } from "@/lib/supabase/server";
export async function GET(_:Request,{params}:{params:Promise<{id:string}>}){await requireRole(["admin","customer_service"]);const {id}=await params;const supabase=await createClient();const {data}=await supabase.from("expense_transactions").select("receipt_path").eq("id",id).maybeSingle();if(!data?.receipt_path)return new NextResponse("Not found",{status:404});const signed=await supabase.storage.from("expense-documents").createSignedUrl(data.receipt_path,300);if(!signed.data?.signedUrl)return new NextResponse("Cannot open",{status:404});return NextResponse.redirect(signed.data.signedUrl);}
