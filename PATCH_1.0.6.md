# ZE CenterOS v1.0.6 — Teacher/Student Mapping

- Adds visible teacher names to student class cards and session history.
- Adds visible student roster to teacher dashboard and class cards.
- Includes SQL repair script to restore class-teacher, session-teacher and enrollment relations from staging/imported records.
- Does not expose finance data to teachers.
