# Apply ZE CenterOS hotfix v1.0.4

Copy every file and folder in this package into the repository root and choose Replace/Overwrite.
Commit the changes to `main`. Vercel will create a new deployment automatically.

Expected behavior without Supabase variables: the site opens `/setup` instead of an Internal Server Error.
